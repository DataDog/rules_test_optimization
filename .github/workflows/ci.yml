name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "feature/**"

jobs:
  bazel-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Bazel tests
        run: ./bazelw test //tools/...
