name: rules_go variant extended

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

permissions:
  contents: read

concurrency:
  group: rules-go-variant-extended-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rules-go-variant-extended:
    timeout-minutes: 45
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"

      - name: Set up Go
        uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00 # v6.0.0
        with:
          go-version: "1.25.0"

      - name: Install Bazelisk
        shell: bash
        run: |
          GO111MODULE=on go install github.com/bazelbuild/bazelisk@v1.28.1
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Run extended vendored rules_go base coverage
        shell: bash
        run: RULES_GO_VARIANT=base ./tools/dev/run_rules_go_variant_extended.sh

      - name: Run extended vendored rules_go complete coverage
        shell: bash
        run: RULES_GO_VARIANT=complete ./tools/dev/run_rules_go_variant_extended.sh
